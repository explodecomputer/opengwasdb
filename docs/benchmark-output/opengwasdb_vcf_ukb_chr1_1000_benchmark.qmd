---
title: "UKB chr1 Dense Store — 1000-Analysis Benchmark"
subtitle: "chr1 × 1000 UKB analyses · Dense Observed-Only store"
date: today
format:
  html:
    toc: true
    toc-depth: 3
    embed-resources: true
    theme: cosmo
jupyter: python3
---

```{python}
#| echo: false
import json
from pathlib import Path

HERE = Path(".")

def load_json(path):
    p = Path(path)
    return json.loads(p.read_text()) if p.exists() else None

bench1k = load_json(HERE / "opengwasdb_vcf_ukb_chr1_1000_benchmark.json")
bench100 = load_json(HERE / "opengwasdb_vcf_ukb_chr1_benchmark.json")

# Raw source VCF sizes measured directly on disk (du -sb on the source manifests).
# See benchmarks/README.md for provenance.
RAW_VCF_MB_100 = 1308.16
RAW_VCF_MB_1000 = 12632.86

PATTERNS = ["regional", "phewas", "tophits", "bulk", "random_lookup"]

PATTERN_DESC = {
    "regional": "All finite associations in a 1 Mb genomic window, all analyses",
    "phewas": "One variant × all 1000 analyses",
    "tophits": "All GW-significant (p < 5×10⁻⁸) associations for one analysis",
    "bulk": "All finite associations for one analysis (~762 K variants)",
    "random_lookup": "100 random variants × 10 random analyses",
}

def timing(data, pattern, field="median_ms"):
    if data is None:
        return None
    for t in data.get("timings", []):
        if t["query"] == pattern:
            return t.get(field)
    return None

def fmt_ms(v):
    if v is None:
        return "n/a"
    return f"{v:,.1f}"

def md_table(headers, rows):
    sep = "|" + "|".join(["---:" if i else "---" for i in range(len(headers))]) + "|"
    head = "| " + " | ".join(headers) + " |"
    body = "\n".join("| " + " | ".join(str(c) for c in row) + " |" for row in rows)
    return f"{head}\n{sep}\n{body}"
```

## Overview

This benchmark measures a **Dense Observed-Only Store** built from **1000 UK Biobank
GWAS-VCF analyses** covering chromosome 1. It is the large-scale companion to the
100-analysis benchmark (`opengwasdb_vcf_ukb_chr1_benchmark.qmd`): the same 762,080
variants and the same source VCFs, with 10× the number of analyses. Its purpose is
to show how build time, storage, and query latency scale with analysis count.

---

## Dataset

```{python}
#| echo: false
#| output: asis
if bench1k:
    d = bench1k["dataset"]
    b = bench1k["build"]
    s = bench1k["storage"]
    bs = b.get("build_seconds")
    build_str = f"{bs/60:.1f} min ({bs:,.0f} s)" if bs else "not measured"
    rows = [
        ("Variants (hg38)", f"{d['n_variants']:,}"),
        ("Analyses", f"{d['n_analyses']:,}"),
        ("Store size", f"{s['store_mb']:,.1f} MB"),
        ("Build time", build_str),
        ("Source assembly", "GRCh37 / hg19 (inline liftover to hg38)"),
        ("Layout", "Dense Observed-Only (`z`, `se` float16 zarr)"),
        ("Store path", f"`{b['store_path']}`"),
    ]
    print(md_table(["Field", "Value"], rows))
else:
    print("_1000-analysis benchmark JSON not found — run "
          "`benchmark_vcf_ukb_chr1_1000_dense.py --rebuild`._")
```

---

## Storage

```{python}
#| echo: false
#| output: asis
if bench1k:
    store_mb = bench1k["storage"]["store_mb"]
    ratio = RAW_VCF_MB_1000 / store_mb
    rows = [
        ("Raw source VCF (.vcf.gz)", f"{RAW_VCF_MB_1000:,.1f} MB", "1.00×"),
        ("OpenGWASDB Dense store", f"**{store_mb:,.1f} MB**", f"**{ratio:.2f}× smaller**"),
    ]
    print(md_table(["Form", "Size", "vs raw"], rows))
    print()
    print(f"The Dense store is **{ratio:.1f}× smaller** than the raw bgzipped source "
          f"VCFs it was built from. The store holds two float16 statistic arrays "
          f"(`z`, `se`) plus a tabix variant axis, an mmap'd ALID search index, and "
          f"a pre-computed top-hit index.")
```

---

## Query timings

All timings are the median over 10 repetitions after one warm-up call. `bulk` and
`random_lookup` deliberately stress the two worst-case access patterns (a full
single-analysis scan and scattered random cell reads).

```{python}
#| echo: false
#| output: asis
if bench1k:
    rows = []
    for p in PATTERNS:
        med = timing(bench1k, p, "median_ms")
        p95 = timing(bench1k, p, "p95_ms")
        cnt = timing(bench1k, p, "result_count")
        rows.append((
            p,
            fmt_ms(med),
            fmt_ms(p95),
            f"{cnt:,}" if cnt is not None else "n/a",
            PATTERN_DESC[p],
        ))
    print(md_table(
        ["Pattern", "Median ms", "p95 ms", "Result cells", "What it reads"],
        rows,
    ))
```

Latency-sensitive point queries (`phewas`, `tophits`) stay in single-digit
milliseconds even at 1000 analyses, because the zarr read per row is amortised
across more columns per chunk. The `bulk` pattern is `O(n_variants)` and reads all
762 K rows for one analysis, so it dominates at several seconds; it is the pattern
to design around rather than optimise blindly.

---

## Scaling: 100 → 1000 analyses

Same 762,080 variants, same chr1 source VCFs, 10× the analyses.

```{python}
#| echo: false
#| output: asis
if bench1k and bench100:
    s100 = bench100["storage"]["store_mb"]
    s1k = bench1k["storage"]["store_mb"]
    bs100 = bench100["build"].get("build_seconds")
    bs1k = bench1k["build"].get("build_seconds")

    def ratio_str(a, b):
        return f"{a/b:.1f}×" if a and b else "n/a"

    rows = [
        ("Analyses", "100", "1,000", "10×"),
        ("Raw source VCF",
         f"{RAW_VCF_MB_100:,.0f} MB", f"{RAW_VCF_MB_1000:,.0f} MB",
         ratio_str(RAW_VCF_MB_1000, RAW_VCF_MB_100)),
        ("Store size",
         f"{s100:,.0f} MB", f"{s1k:,.0f} MB",
         ratio_str(s1k, s100)),
        ("Build time",
         f"{bs100/60:.1f} min" if bs100 else "n/a",
         f"{bs1k/60:.1f} min" if bs1k else "n/a",
         ratio_str(bs1k, bs100) if (bs100 and bs1k) else "n/a"),
    ]
    print(md_table(["", "100 analyses", "1000 analyses", "Ratio"], rows))
elif bench100 is None:
    print("_100-analysis benchmark JSON not found — cannot compute scaling table._")
```

```{python}
#| echo: false
#| output: asis
if bench1k and bench100:
    rows = []
    for p in PATTERNS:
        t100 = timing(bench100, p, "median_ms")
        t1k = timing(bench1k, p, "median_ms")
        rc100 = timing(bench100, p, "result_count")
        rc1k = timing(bench1k, p, "result_count")
        tr = f"{t1k/t100:.1f}×" if t100 and t1k else "n/a"
        cr = f"{rc1k/rc100:.1f}×" if rc100 and rc1k else "n/a"
        rows.append((
            p, fmt_ms(t100), fmt_ms(t1k), tr,
            f"{rc100:,}" if rc100 else "n/a",
            f"{rc1k:,}" if rc1k else "n/a",
            cr,
        ))
    print(md_table(
        ["Pattern", "100 (ms)", "1000 (ms)", "Time ratio",
         "Cells (100)", "Cells (1000)", "Cell ratio"],
        rows,
    ))
```

Storage and build time both scale **sub-linearly** with analysis count — the
variant axis, tabix index, and ALID index are fixed overhead paid once and
amortised across every additional analysis column. Query latency scales with the
result-set size: `regional` and `bulk` read more cells so grow closer to linearly,
while `phewas` and `tophits` scale better because each per-row zarr read serves
more columns per chunk.

---

## Provenance

```{python}
#| echo: false
#| output: asis
lines = []
if bench1k:
    bs = bench1k["build"].get("build_seconds")
    build_str = f"built in {bs:,.0f}s" if bs else "existing store reused"
    lines.append(
        f"- 1000-analysis timings: `opengwasdb_vcf_ukb_chr1_1000_benchmark.json` "
        f"({build_str}, {bench1k['dataset']['n_variants']:,} variants × "
        f"{bench1k['dataset']['n_analyses']:,} analyses)"
    )
if bench100:
    lines.append(
        "- 100-analysis timings (scaling comparison): "
        "`opengwasdb_vcf_ukb_chr1_benchmark.json`"
    )
lines.append(
    "- Raw VCF sizes: `du -sb` on source manifests "
    "`besdq/data/vcf-ukb{,-1000}/*.vcf.gz` (see `benchmarks/README.md`)"
)
lines.append(
    "- Benchmark script: `benchmarks/benchmark_vcf_ukb_chr1_1000_dense.py`"
)
print("\n".join(lines))
```
